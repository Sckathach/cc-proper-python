# CC course : Great habits with Python


## Installation
Create a virtual environment and add dependencies:
```sh 
python3 -m venv venv
source venv/bin/activate
pip install -r requirements
```

> The configuration can be found in `.env`.

## Use 
Run the tests:
```sh 
pytest
```
